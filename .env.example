# Plugin Signing Configuration
# These variables are used for plugin signing during the build process.
# For local development, copy this file to .env and fill in the values.
# For CI/CD, set these as GitHub Actions secrets.

# Base64-encoded plugin certificate (PEM format)
# Generate with: base64 -w0 plugin-signing.crt
CERTIFICATE_CHAIN=

# Base64-encoded plugin private key (PEM format)
# Generate with: base64 -w0 plugin-signing.key
PRIVATE_KEY=

# Password for the private key (leave empty if key has no password)
PRIVATE_KEY_PASSWORD=

# JetBrains Marketplace publish token
# Obtain from: https://plugins.jetbrains.com/oauth/authorize?...
PUBLISH_TOKEN=
